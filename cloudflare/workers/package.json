{
  "name": "ths-auto-organizer-workers",
  "version": "2.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev src/index.js --port 8787",
    "deploy": "wrangler deploy src/index.js",
    "test": "vitest run"
  },
  "dependencies": {
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "vitest": "^2.0.0",
    "wrangler": "^4.136.3"
  }
}
