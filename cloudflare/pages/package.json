{
  "name": "ths-auto-organizer-pages",
  "version": "2.1.0",
  "private": true,
  "scripts": {
    "dev": "npx -y serve src -l 3000",
    "build": "node scripts/build.js",
    "deploy": "npx wrangler pages deploy dist --project-name=ths-organizer"
  },
  "devDependencies": {
    "wrangler": "^3.80.0"
  }
}
